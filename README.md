# lighthouse-v2

Here used audit.js to read urls and generate lighthouse reports and passes the reports by grouping issues also before sending it to ai it process batch by batch to avoid model overload 
